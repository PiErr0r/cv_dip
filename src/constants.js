// math
const { PI } = Math;
const EPS = 0.0001

// movement
const FPS = 15;
const DX = 0.05;
const DY = 0.05;
const DZ = 0.05;
const DTHETA = 0.1;
const SPEED = 1/FPS;

// canvas
const POINT_SIZE = 5;
const LINE_WIDTH = 1;
const DEFAULT_COLOR = "#000000";
const PT_SIZE = 0.05;
const MAX_DISTANCE = 1.5;

// camera
const F = 2.4;
const SCALE = 2;
const W = 16 / SCALE;
const H = 9 / SCALE;
